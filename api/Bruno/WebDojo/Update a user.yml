info:
  name: Update a user
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3333/api/users/1000
  body:
    type: json
    data: |-
      {
        "name": "miranha",
        "email": "plankton@teste.com.br",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
